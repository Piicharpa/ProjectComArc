label > 6 character thorw err
เช็คคำผิด คำเกิน 
1. การใช้labels ที่undefine -ตอนติด ตอนใช้
2. การใช้label ที่เหมือนกัน
3. การใช้offsetField ที่มีจำนวน bit มากกว่า 16 bits
4. การใช้opcode นอกเหนือจากที่กำหนด

starttt /

bcAdr   .fill   bc
        .fill   0    
        .fill   0    
        .fill   0
        .fill   0  